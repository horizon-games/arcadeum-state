{
  "name": "arcadeum-rps",
  "version": "0.0.0",
  "description": "Arcadeum rock-paper-scissors WebAssembly module",
  "main": "src/index.js",
  "author": "Horizon Blockchain Games",
  "license": "MIT",
  "scripts": {
    "build": "cargo build --release --target=wasm32-unknown-unknown && wasm-bindgen --out-dir src --out-name index --nodejs target/wasm32-unknown-unknown/release/arcadeum_rps_bindings.wasm"
  }
}
